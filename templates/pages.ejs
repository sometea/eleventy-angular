---
permalink: "/api/pages/index.json"
---
<% const pages = collections.pages.map(page => ({ title: page.data.title, url: `/api/pages/${page.fileSlug}.json`, fileSlug: page.fileSlug, date: page.date })); %>
<%- JSON.stringify({pages}); -%>